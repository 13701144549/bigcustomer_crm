#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.7.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import Remit2
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host:port] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  i64 create_remit_bat(i64 remit_type_id, i64 account_type_id)'
  print '  i32 remit(i64 bat_id)'
  print '  i32 remit_audit_pass(i64 bat_id)'
  print '  i32 remit_audit_refuse(i64 bat_id)'
  print '  i32 generate_excel(i64 bat_id)'
  print '  i64 create_remitback_bat(i64 remitback_type_id)'
  print '  i32 remitback(RemitBackArgs remit_back_args)'
  print '  i32 remitback_audit_pass(i64 bat_id)'
  print '  i32 remitback_audit_refuse(i64 bat_id)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = Remit2.Client(protocol)
transport.open()

if cmd == 'create_remit_bat':
  if len(args) != 2:
    print 'create_remit_bat requires 2 args'
    sys.exit(1)
  pp.pprint(client.create_remit_bat(eval(args[0]),eval(args[1]),))

elif cmd == 'remit':
  if len(args) != 1:
    print 'remit requires 1 args'
    sys.exit(1)
  pp.pprint(client.remit(eval(args[0]),))

elif cmd == 'remit_audit_pass':
  if len(args) != 1:
    print 'remit_audit_pass requires 1 args'
    sys.exit(1)
  pp.pprint(client.remit_audit_pass(eval(args[0]),))

elif cmd == 'remit_audit_refuse':
  if len(args) != 1:
    print 'remit_audit_refuse requires 1 args'
    sys.exit(1)
  pp.pprint(client.remit_audit_refuse(eval(args[0]),))

elif cmd == 'generate_excel':
  if len(args) != 1:
    print 'generate_excel requires 1 args'
    sys.exit(1)
  pp.pprint(client.generate_excel(eval(args[0]),))

elif cmd == 'create_remitback_bat':
  if len(args) != 1:
    print 'create_remitback_bat requires 1 args'
    sys.exit(1)
  pp.pprint(client.create_remitback_bat(eval(args[0]),))

elif cmd == 'remitback':
  if len(args) != 1:
    print 'remitback requires 1 args'
    sys.exit(1)
  pp.pprint(client.remitback(eval(args[0]),))

elif cmd == 'remitback_audit_pass':
  if len(args) != 1:
    print 'remitback_audit_pass requires 1 args'
    sys.exit(1)
  pp.pprint(client.remitback_audit_pass(eval(args[0]),))

elif cmd == 'remitback_audit_refuse':
  if len(args) != 1:
    print 'remitback_audit_refuse requires 1 args'
    sys.exit(1)
  pp.pprint(client.remitback_audit_refuse(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
