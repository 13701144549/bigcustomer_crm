#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.7.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import Check2
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host:port] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  i16 ping()'
  print '  i64 create_bat(i32 chnl_id, string date)'
  print '  i32 download_chnlrecord(i64 bat_id)'
  print '  i32 import_chnlrecord(i64 bat_id)'
  print '  i32 import_bizrecord(i64 bat_id)'
  print '  i32 check(i64 bat_id)'
  print '  i32 mistake_handle( params)'
  print '  i32 audit(i64 bat_id, i32 result)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = Check2.Client(protocol)
transport.open()

if cmd == 'ping':
  if len(args) != 0:
    print 'ping requires 0 args'
    sys.exit(1)
  pp.pprint(client.ping())

elif cmd == 'create_bat':
  if len(args) != 2:
    print 'create_bat requires 2 args'
    sys.exit(1)
  pp.pprint(client.create_bat(eval(args[0]),args[1],))

elif cmd == 'download_chnlrecord':
  if len(args) != 1:
    print 'download_chnlrecord requires 1 args'
    sys.exit(1)
  pp.pprint(client.download_chnlrecord(eval(args[0]),))

elif cmd == 'import_chnlrecord':
  if len(args) != 1:
    print 'import_chnlrecord requires 1 args'
    sys.exit(1)
  pp.pprint(client.import_chnlrecord(eval(args[0]),))

elif cmd == 'import_bizrecord':
  if len(args) != 1:
    print 'import_bizrecord requires 1 args'
    sys.exit(1)
  pp.pprint(client.import_bizrecord(eval(args[0]),))

elif cmd == 'check':
  if len(args) != 1:
    print 'check requires 1 args'
    sys.exit(1)
  pp.pprint(client.check(eval(args[0]),))

elif cmd == 'mistake_handle':
  if len(args) != 1:
    print 'mistake_handle requires 1 args'
    sys.exit(1)
  pp.pprint(client.mistake_handle(eval(args[0]),))

elif cmd == 'audit':
  if len(args) != 2:
    print 'audit requires 2 args'
    sys.exit(1)
  pp.pprint(client.audit(eval(args[0]),eval(args[1]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
