#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.7.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import Fund2
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host:port] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '   remit_query(FundQueryParams query_args)'
  print '   settle_query(FundQueryParams query_args)'
  print '   trade_query(FundQueryParams query_args)'
  print '  i32 remit_query_count(FundQueryParams query_args)'
  print '  i32 add_remit_rule( rule_args)'
  print '  i32 change_remit_rule( rule_args)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = Fund2.Client(protocol)
transport.open()

if cmd == 'remit_query':
  if len(args) != 1:
    print 'remit_query requires 1 args'
    sys.exit(1)
  pp.pprint(client.remit_query(eval(args[0]),))

elif cmd == 'settle_query':
  if len(args) != 1:
    print 'settle_query requires 1 args'
    sys.exit(1)
  pp.pprint(client.settle_query(eval(args[0]),))

elif cmd == 'trade_query':
  if len(args) != 1:
    print 'trade_query requires 1 args'
    sys.exit(1)
  pp.pprint(client.trade_query(eval(args[0]),))

elif cmd == 'remit_query_count':
  if len(args) != 1:
    print 'remit_query_count requires 1 args'
    sys.exit(1)
  pp.pprint(client.remit_query_count(eval(args[0]),))

elif cmd == 'add_remit_rule':
  if len(args) != 1:
    print 'add_remit_rule requires 1 args'
    sys.exit(1)
  pp.pprint(client.add_remit_rule(eval(args[0]),))

elif cmd == 'change_remit_rule':
  if len(args) != 1:
    print 'change_remit_rule requires 1 args'
    sys.exit(1)
  pp.pprint(client.change_remit_rule(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
